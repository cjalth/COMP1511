// loads the standard input/output library
#include<stdio.h>

// main function
int main(void) {

    // prints the string to the standard output
    printf("Hello, world!\n");

    // returns 0 to the operating system
    return 0;

// code "codename.c" to open the file
// dcc "code name" -o "code name without .c" compiles code
// ./"code name" runs the code

}